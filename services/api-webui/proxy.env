# Proxy defaults for /v1/chat/completions
# These are applied to every request sent to the backend
# Spec decode requires temperature=0 and top_p=1.0 (no logit processing)

DEFAULT_MAX_TOKENS=1024
DEFAULT_TEMPERATURE=0
DEFAULT_TOP_P=1
